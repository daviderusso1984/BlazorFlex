namespace Test
{

    public enum Directions
    {
        row, row_reverse, column, column_reverse
    }

    public enum Vertical_aligns
    {
        start, center, end, stretch, baseline
    }

    public enum Horizontal_alings
    {
        start, center, end, space_around, space_between
    }

    //


}